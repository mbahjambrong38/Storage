package com.baru.storage;

import androidx.appcompat.app.AppCompatActivity;

import android.os.Bundle;

public class ViewStock extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_view_stock);

//        Log.d("VEC MATERIAL", String.format("TOTAL VEC MATERIAL : %d", db.VEC_MATERIAL.size()));
//        Log.d("VEC PLANT", String.format("TOTAL VEC PLANT : %d", db.VEC_PLANT.size()));
//        Log.d("VEC STOCK", String.format("TOTAL VEC STOCK : %d", db.VEC_STOCK.size()));
//        Log.d("VEC STORAGE LOCATION", String.format("TOTAL VEC STORAGE LOCATION : %d", db.VEC_STORAGE_LOCATION.size()));
    }
}